import React from "react";
import { Modal, ModalDialog, Typography, Button, Input, CircularProgress, Option, Box, Card } from "@mui/joy";

const ProfesionalesVista = () => {

    return (

        <Box >
            <Card sx={{ boxShadow: "lg" }}>
                <Typography>Aca el listado de profesionales</Typography>
            </Card>
        </Box>

    );
};

export default ProfesionalesVista;
